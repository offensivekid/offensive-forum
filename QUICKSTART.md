# 🚀 QUICKSTART - offensive-forum v2.0

## За 5 минут до запуска!

### 1️⃣ Установи зависимости

```bash
npm install
```

### 2️⃣ Настрой админа (ВАЖНО!)

Открой `.env` и измени:

```env
ADMIN_USERNAME=admin
ADMIN_PASSWORD=твой-сильный-пароль-здесь
ADMIN_EMAIL=твой@email.com
```

### 3️⃣ Инициализируй БД

```bash
npm run init-db
```

**ЗАПОМНИ КЛЮЧ** который появится в консоли! Формат: `XXXX-XXXX-XXXX-XXXX`

### 4️⃣ Запусти

```bash
npm start
```

### 5️⃣ Открой браузер

```
http://localhost:3000
```

## 🎯 Что дальше?

### Зарегистрируй тестового юзера:

1. Нажми "Login" → "Register"
2. Заполни форму
3. Регистрация!

### Войди как обычный юзер:

- Username: твой username
- Password: твой password
- Access Key: **оставь пустым**
- Результат: видишь только публичные темы

### Войди с ключом доступа:

- Username: твой username
- Password: твой password
- Access Key: **вставь ключ из шага 3**
- Результат: видишь ВСЕ темы (публичные + приватные)!

### Войди как админ:

- Username: `admin` (или что указал в .env)
- Password: твой пароль из .env
- Результат: админ-панель + корона 👑

## 📋 Что работает:

✅ **Регистрация/Логин** с bcrypt хэшированием
✅ **Публичные темы** - создают все зарегистрированные
✅ **Приватные темы** - создают только админы, видят с ключом
✅ **Access Keys** - one-time use ключи для доступа
✅ **Админ-панель** - генерация ключей, статистика
✅ **SIEM logging** - все события в БД
✅ **2 языка** - EN/RU переключение

## 🔑 Система ключей:

### Как работает:

1. **Админ**: Заходит → Admin Panel → Generate Keys → копирует ключи
2. **Админ**: Раздаёт ключи нужным людям (в личку, на форуме)
3. **Юзер**: Регистрируется обычным способом
4. **Юзер**: Логинится **с ключом** → получает доступ к приватным темам
5. **Система**: Ключ деактивируется (one-time use)

### Генерация ключей:

```
Admin Panel → Number of keys: 10 → Generate
```

Получишь список из 10 ключей типа:
```
ABCD-1234-EFGH-5678
IJKL-9012-MNOP-3456
...
```

## ⚠️ ВАЖНО

1. **СМЕНИ ПАРОЛЬ АДМИНА** в .env
2. **СМЕНИ SESSION_SECRET** в .env
3. **Делай backup** файла database.sqlite
4. **Для production** используй HTTPS

## 🔧 Команды:

```bash
npm start          # Запуск
npm run dev        # Разработка (nodemon)
npm run init-db    # Инициализация БД
```

## 💡 Подсказки:

- **Забыл пароль админа?** → Удали `database.sqlite` → запусти `npm run init-db`
- **Нужны новые ключи?** → Войди как админ → Admin Panel → Generate
- **Хочешь дать приватный доступ?** → Сгенерируй ключ → отправь юзеру
- **Юзер потерял ключ?** → Сгенерируй новый (старые one-time)

## 📊 Типы пользователей:

| Тип | Видит публичные | Видит приватные | Создаёт публичные | Создаёт приватные | Генерирует ключи |
|-----|----------------|----------------|------------------|------------------|-----------------|
| **Не залогинен** | ❌ | ❌ | ❌ | ❌ | ❌ |
| **Обычный юзер** | ✅ | ❌ | ✅ | ❌ | ❌ |
| **С ключом** | ✅ | ✅ | ✅ | ❌ | ❌ |
| **Админ** | ✅ | ✅ | ✅ | ✅ | ✅ |

## 🎉 Готово!

Всё настроено, форум работает!

**Следующие шаги:**
1. Создай несколько публичных тем
2. Создай приватную тему (как админ)
3. Сгенерируй ключи
4. Раздай ключи тестовым юзерам
5. Проверь что они видят приватные темы!

---

**Время установки: ~5 минут**
**Версия: 2.0 - Local Database Edition**
